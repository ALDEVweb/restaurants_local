{% extends 'base.html.twig' %}

{% block title %}New Restaurant{% endblock %}

{% block body %}
<div>
    <h1 class="txt-center mb32">Create new Restaurant</h1>

    {{ include('restaurant/_form.html.twig') }}

    <div class="mt16">
        <a href="{{ path('app_restaurant_index') }}">back to list</a>
    </div>
</div>
{% endblock %}
