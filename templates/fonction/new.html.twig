{% extends 'base.html.twig' %}

{% block title %}New Fonction{% endblock %}

{% block body %}
<div>
    <h1 class="txt-center mb32">Create new Fonction</h1>

    {{ include('fonction/_form.html.twig') }}
    <div class="mt16">
        <a href="{{ path('app_fonction_index') }}">back to list</a>
    </div>
</div>
{% endblock %}
