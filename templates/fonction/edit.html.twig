{% extends 'base.html.twig' %}

{% block title %}Edit Fonction{% endblock %}

{% block body %}
<div>
    <h1 class="txt-center mb32">Edit Fonction</h1>
        {{ include('fonction/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="flex a-center gap16 mt16">
        <a href="{{ path('app_fonction_index') }}">back to list</a>

        {{ include('fonction/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
